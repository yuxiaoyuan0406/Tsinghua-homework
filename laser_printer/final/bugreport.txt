传输过程存在丢包，可能是SD卡写入过程耗时太长而又处在中断之中。

尝试过使用字符串暂存数据，发送完毕后再写入到SD卡。出现了无法收到数据的问题

现在用结尾的（0,0,1）点作为结束标记，检测到该点则向服务器发送报告，服务器发出终止指令。

扫描完毕后单片机发送的信息服务器出现收不到的情况。

在服务器代码中加入了循环，若上一个收到的报告是wait data. 则什么也不做，仍解决不了问题2。
